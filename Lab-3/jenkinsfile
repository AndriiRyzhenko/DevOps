pipeline {
    agent any

    options {
        timestamps()
    }

    stages {
        stage('Build Docker Image') {
            steps {
                sh '''
                ls
                cd ../
                ls
                cd "Lab-1"
                ls
                docker build -t build-lab-1 .
                '''
            }
        }
    }
}