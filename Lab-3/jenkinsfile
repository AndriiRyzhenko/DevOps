pipeline {
    agent any

    options {
        timestamps()
    }

    stages {
        stage('Build Docker Image') {
            steps {
                sh '''
                cd ../
                ls
                cd "Lab-1"
                docker build -t build-lab-1 .
                '''
            }
        }
    }
}